# Root .env.example
# Public Sanity values used by the site (safe to commit as examples)
PUBLIC_SANITY_PROJECT_ID=puh7k8ji
PUBLIC_SANITY_DATASET=production

# Add other local-only env entries below
# e.g. API_URL=http://localhost:3000
